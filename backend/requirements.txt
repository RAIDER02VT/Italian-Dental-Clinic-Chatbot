# ==== TekVal unified requirements (Python 3.12, WSL) ====
# Prende i wheel CPU di PyTorch dall'index ufficiale:
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-dotenv>=1.0,<2.0
openai>=1.44,<2.0

# ML runtime (senza pin rigidi; CPU)
torch
# Se li usi davvero, scommenta:
# torchvision
# torchaudio

# Networking / ASGI (alcuni progetti li usano):
# httpx>=0.27
# starlette>=0.36,<1.0
pandas>=2.2
chromadb>=0.5
